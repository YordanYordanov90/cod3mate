# Progress Tracker

Update this file after every meaningful implementation change.

## Dual-Branch Model

Progress spans two git branches and two deploy targets:

- **`main` → Railway:** Telegram agent, tools, QA persistence, Railway dashboard API. Track agent milestones and QA roadmap phases here.
- **`feature/dashboard` → Vercel:** Next.js dashboard (`apps/dashboard`). Track dashboard milestones (M0–M12 in `context/dashboard.md`) here.

The branches do not need identical code, but **`context/` should stay the same** on both so planning does not drift. After backend work on `main`, merge into `feature/dashboard` when the dashboard needs updated API behavior. See `context/dashboard.md` → **Branch And Deployment Workflow**.

## Status Legend

- ✅ Done
- 🟡 In progress
- ⬜ Not started
- ⚠️ Blocker / needs decision
- 🔵 Resolved (for Open Questions)

## Current Phase

- ✅ Milestone 1 (Foundation) complete.
- ✅ Milestone 2 (Telegram Bot Shell) complete.
- ✅ Milestone 3 (SOUL and Sessions) complete.
- ✅ Milestone 4 (OpenAI Agent Loop) complete.
- ✅ Milestone 5 (Tool Registry) complete.
- ✅ Milestone 6 (Core Tools) complete and fully tested.
- ✅ Milestone 7 (Telegram Task Summary) complete (later consolidated — see M8 notes).
- ✅ Milestone 8 (Railway Deployment) complete. Bot is live for the whitelisted owner.

## Current Goal

- All milestones from the original plan are complete. Bot is in active personal use. Future work is incremental hardening and feature requests as they come up.

## Completed

- ✅ Created project-specific planning context for:
  - ✅ project overview
  - ✅ architecture
  - ✅ code standards
  - ✅ AI workflow rules
  - ✅ progress tracking
  - ✅ Telegram UI context
- ✅ Replaced the persisted Markdown + GitHub report system with Telegram-only task summaries for v1. No files saved.
- ✅ **Milestone 1: Foundation** — full TypeScript/Node project bootstrap, strict env validation, folder structure, build/test pipeline, and safe startup logging (no secret values ever emitted). All exit criteria from ai-workflow-rules.md Phase 1 satisfied.
- ✅ **Milestone 2: Telegram Bot Shell** — GrammY bot with owner-only whitelist middleware (first in chain), 5 command shells, pure chunking utility, long-polling startup, graceful shutdown, and comprehensive unit tests. Exit criteria met: non-owners blocked before handlers, chunking works, owner receives clean responses.
- ✅ **Milestone 3: SOUL and Sessions** — Persistent `/data` storage layer, SOUL.md with safe default creation, full per-chat JSON session management, working /reset, history capture on messages, and dependency injection into the bot. Exit criteria met: app starts with SOUL, sessions can be loaded/saved/reset.
- ✅ **Milestone 4: OpenAI Agent Loop** — Full OpenAI integration with client wrapper, security-first + SOUL system prompts, primary/fallback handling, per-chat model switching via /model, and real responses for owner messages. Exit criteria met: owners get model answers, /model works for switching, fallback tested with mocks.
- ✅ **Milestone 5: Tool Registry** — Complete shared tool interface, Zod-validated registry, safe execution (timeout + truncation), credential sanitization on all outputs, and agent runner support for tool calling. Exit criteria met: registry can be called, invalid calls fail safely, results are sanitized before reaching the model.
- ✅ **Milestone 6: Core Tools** — All 9 production tools implemented and registered (file_read, file_write, terminal_exec, web_search, browser_navigate/click/fill/screenshot/extract_text). Dedicated unit tests for files (traversal + integration), terminal (allowlist + cwd sandbox + sibling-prefix bypass), and search (mocked Tavily, error paths, key non-leakage). Registry now honors MAX_TOOL_OUTPUT_CHARS from env. Bot `/start` + `/help` updated to reflect live capabilities.
- ✅ **Milestone 7: Telegram Task Summary** — `buildTaskSummary` implemented in `src/summary/mod.ts` producing the exact required structure (short title, one-paragraph Result, Tools used, Caveats, optional Next steps). Full credential sanitization on the summary and all outgoing Telegram responses. Originally delivered as a separate "Working on it..." / "Done." / summary message. **Consolidated during M8** into a single merged Telegram message (answer + compact `—` footer with tools used / failures / iteration-limit notice) because the structured summary repeated the answer paragraph on mobile. The 9 unit tests for `buildTaskSummary` still pass; the function is retained for potential future reuse but is not currently wired into the bot.
- ✅ **Milestone 8: Railway Deployment** — Multi-stage Dockerfile (Node 20 builder → `mcr.microsoft.com/playwright:v1.52.0-jammy` runtime, deps pruned, browser download skipped in builder). `.dockerignore` excludes `.env*`, runtime data, tests, docs, and editor noise. Repo pushed to GitHub; Railway service deploys from `main` automatically. Persistent volume mounted at `/data`. Custom `SOUL.md` uploaded to `/data/SOUL.md` via `railway ssh` after registering the local SSH key with Railway. All required env vars configured in Railway Variables. Bot is online and operational for the whitelisted owner.
- ✅ **Multi-round tool loop** — `runAgent` rewritten as a true bounded `for` loop over `MAX_AGENT_ITERATIONS`. Each iteration is one model call; tool results append and another iteration runs until the model returns a tool-free answer or the limit is hit. Fallback to `OPENAI_FALLBACK_MODEL` is now first-iteration-only (after any tool result, the active model is locked). `iterationLimitHit` is surfaced in the merged-message footer. All 94 tests pass.
- ✅ **Test credentials feature** — `TEST_ACCOUNT_EMAIL` / `TEST_ACCOUNT_PASSWORD` optional env pair. When both are set, the agent receives them through an elevated-priority block in the system prompt (above SOUL, equal priority to security rules) with strict no-echo policy. Both literal values are registered with the central sanitizer at startup so any leak is redacted to `[REDACTED]`. Credentials are never accepted from chat input; the agent refuses any flow that would require the owner to type them. Used silently inside `browser_fill` for live application testing.

## Recently Completed

- Single merged Telegram response per task (replaces 3-message flow).
- True multi-round agent loop bounded by `MAX_AGENT_ITERATIONS`.
- Test-credentials block (`TEST_ACCOUNT_EMAIL` / `TEST_ACCOUNT_PASSWORD`) wired through prompt + sanitizer.
- Railway deployment with Dockerfile, `/data` volume, and custom SOUL.md uploaded over SSH.
- Phase 1 of QA Agent Roadmap: 5 structured assertion tools (`qa_assert_visible`, `qa_assert_not_visible`, `qa_assert_text_contains`, `qa_assert_url`, `qa_assert_element_count`) + shared browser locator helpers + 13 new tests. All return `{passed, expected, actual, message}`; cross-frame supported; build/test/typecheck green (total 107 tests).
- Phase 2 of QA Agent Roadmap: QA Report Collector + Formatter in `src/tools/qa/report.ts` (start/record/end, auto-wired from assertions via optional `name` labels + derived names + timing), persistence under `/data/qa-reports/` via new `src/storage/qa-reports.ts` (save + listRecent summaries), `/qa-history` Telegram command, report auto-emitted as follow-up message (sanitized) for any run that executes qa_assert_* tools (plus error-path handling), updated help/status/start. 7 dedicated report tests + 114 total tests green. Foundation for explicit /qa-test (Phase 5) and scenarios (Phase 7).
- Phase 3 of QA Agent Roadmap: Network & Console Observation — Playwright event listeners (console error/warn, requestfailed, response w/ truncated body + 4xx) in browser core; clear on nav/reset; 3 qa_* tools in qa/monitoring.ts; 6 tests (capture, clear-via-nav, truncation); 120 tests total green.
- Phase 4 of QA Agent Roadmap: Wait Tools — browser_wait_for (selector+state+timeout, cross-frame), browser_wait_for_network_idle, browser_wait_for_text (reuses text resolver); return elapsed ms; 6 tests (appear/timeout/text/idle + elapsed); now 126 tests green.
- Phase 5 of QA Agent Roadmap: Raise Iteration Limit for QA — added `/qa-test <url> <desc>` command (parses URL+description), browser reset via `resetBrowserState`, per-run `maxIterations:25` (env default stays 8 for normal chat), refactored shared `processAgentTask` helper in bot.ts for DRY run/report/merged-send logic. Updated /start /help /status. 126 tests + build + typecheck green. (No new tests needed; leverages existing QA paths.)
- Phase 6 of QA Agent Roadmap: Terminal Allowlist Expansion — added `grep`, `curl`, `npx`, `git` (with strict read-only subcommand/flag guards for git + GET-only + no-write guards for curl). Enhanced `isCommandAllowed` + two new validators in `src/tools/terminal/mod.ts`. Updated tool description for the model. Expanded tests (policy + direct guards + exec + blocked variants) + updated docs (AGENTS, README, roadmap, tracker). 126+ tests green; dangerous ops (git push/reset/clone, curl POST/data/-o etc.) still blocked at the gate. 
- Phase 7 of QA Agent Roadmap: Reusable Test Scenarios — full storage under /data/qa-scenarios (save/load/list by name), scenario-runner.ts with typed steps + $TEST_* substitution (runtime only) + executor (via toolRegistry, integrates with report collector for asserts) + qa_save_scenario tool. /qa-scenarios + /qa-run Telegram commands (with browser reset + report emission like /qa-test). Updated ensure paths, bot deps (testCredentials), index registration, /start/help/status. 8 new tests (then 141). No literal creds in storage. Build/test/typecheck green.
- Phase 8 of QA Agent Roadmap: Multi-App Credential Support — env pattern TEST_CREDENTIALS_<APP>_* parsed in config/env (getAppCredentials + passthrough), legacy TEST_ACCOUNT_* preserved. Prompt updated to inject per-app blocks (names listed + values for model use; selection by context like "test on CloudcastAI"). All values registered w/ sanitizer at start. Extended to scenario substitution (supports $TEST_CREDENTIALS_<APP>_* + legacy), save-tool literal-value guard (across all sets), bot/runner threading of appCredentials map. Updated tests/docs (env.test + prompt.test). 143 tests green.
- Phase 9 of QA Agent Roadmap: Viewport & Responsive Testing — added `browser_set_viewport` tool (presets + custom, using persistent page.setViewportSize()). Updated BrowserTools interface + registration in index. Updated help text. New dedicated tests (presets, custom, validation, post-set viewportSize query + screenshot effect). 143 tests green. Screenshots now reflect viewport changes for QA flows.
- Phase 10 of QA Agent Roadmap: Accessibility Auditing — installed @axe-core/playwright. Added `qa_accessibility_audit` tool in src/tools/qa/accessibility.ts (uses AxeBuilder.analyze(), structured by severity, auto-records to active QA report via recordAssertion for inclusion in reports). Registered in index. Updated bot help/status/start. New tests (violations page, clean page, report integration). 150 tests green. All exit criteria met.
- Medium-issue cleanup: synced AGENTS.md, README.md, architecture.md, SOUL.md (env-only test creds), progress-tracker QA section; tightened `npx` allowlist; blocked `git config`; documented test-credential system-prompt exception in invariants.
- High-priority QA UX: AsyncLocalStorage report collector (no cross-task bleed), QA collection only on `/qa-test`, `/qa-run`, or explicit QA phrasing; screenshots auto-sent to Telegram during active QA runs (`src/telegram/qa-ux.ts`).

## Next Up (incremental, no fixed schedule)

- Further terminal expansions only if real QA workloads demonstrate need (Phase 6 added grep/curl/npx/git-read; npx restricted to known runners, `git config` blocked).
- Decide whether to re-wire the structured summary builder into a separate channel (digest / log / per-task artifact) or remove it.
- Optional: webhook mode in front of long-polling once a public hostname is justified.
- Optional: hot reload of `SOUL.md` without a redeploy.

## Implementation Milestones

### ✅ Milestone 1: Foundation

- ✅ `package.json` (ESM, scripts for build/dev/test/typecheck, core deps + dev tooling)
- ✅ `tsconfig.json` (strict mode, NodeNext, noUnused*, exactOptional etc.)
- ✅ source folder structure (full per architecture.md: config, telegram, agent, soul, tools/*, summary, security, storage, lib + tests mirror)
- ✅ env validation (Zod schema for all required + optional vars with defaults; fails fast with actionable messages, zero secret leakage)
- ✅ test runner (Vitest + 3 passing tests covering load, failure modes, redaction)
- ✅ build script (`npm run build` / `typecheck` / `test:run` all green)
- ✅ `.env.example` + `.gitignore` (protects .env, data/, dist/, etc.)
- ✅ basic health startup logging + sanitized summary + graceful shutdown skeleton in `src/index.ts`

### ✅ Milestone 2: Telegram Bot Shell

- ✅ GrammY bot startup with long-polling (fire-and-forget poller + stop fn)
- ✅ Owner whitelist middleware (registered first; silent drop for non-owners per UI rules)
- ✅ `/start`, `/help`, `/status`, `/reset`, `/model` thin command shells (no /report)
- ✅ Telegram formatting + safe chunking utility (pure, well tested, respects TELEGRAM_CHUNK_SIZE)
- ✅ Unit tests for whitelist decision (including middleware integration), chunking, and bot creation

### ✅ Milestone 3: SOUL and Sessions

- ✅ `/data` + sessions directory creation at startup (`ensureDataDirectories`)
- ✅ `/data/SOUL.md` loading with safe default template creation + warning log when missing
- ✅ Per-chat session persistence under `/data/sessions/<chatId>.json` (load/save/reset/append)
- ✅ Real `/reset` command that clears persisted history for the current chat
- ✅ Basic user message history capture in the generic text handler
- ✅ Full DI for session functions into the bot (testable)
- ✅ 33 tests passing, including dedicated suites for storage, sessions, and soul loading

### ✅ Milestone 4: OpenAI Agent Loop

- ✅ OpenAI SDK wrapper (`createOpenAIClient`) with typed chat completions
- ✅ System prompt construction (`buildSystemPrompt`) — security rules first, then SOUL
- ✅ Agent runner (`runAgent`) with primary + fallback logic on retryable errors
- ✅ Conversation history integration (session → OpenAI messages)
- ✅ Per-chat model selection + full `/model` switching support (primary / fallback / custom / clear)
- ✅ Real agent responses for owner text messages (replaces placeholder)
- ✅ Comprehensive mocked tests for prompt, runner, and fallback behavior
- ✅ 38 tests passing, clean build with strict TypeScript

### ✅ Milestone 5: Tool Registry

- ✅ Shared Tool interface + ToolResult normalization (code-standards compliant)
- ✅ ToolRegistry with registration, lookup, Zod input validation
- ✅ OpenAI-compatible tool definition generation (JSON Schema from Zod)
- ✅ Safe execution wrapper (timeout + output truncation)
- ✅ Centralized credential sanitization (`src/security/sanitize.ts`) applied to all tool results
- ✅ Agent runner extended to support tool_calls + tool result feedback loop
- ✅ Dedicated registry + sanitization test suites
- ✅ Registry runtime limits (`maxOutputChars`, `timeoutMs`) configurable at startup; wired to `MAX_TOOL_OUTPUT_CHARS` in `src/index.ts`

### ✅ Milestone 6: Core Tools

- ✅ file_read + file_write with full path traversal protection under TMP_DIR (exported `resolveSafePath`)
- ✅ terminal_exec with conservative allowlist (pwd, ls, echo, node/npm --version, etc.), sanitized env, timeout
- ✅ Hardened terminal cwd sandbox using `root + path.sep` prefix check (closes the `/tmp/agent-files-evil` sibling-prefix bypass)
- ✅ Full browser toolset (navigate, click, fill, screenshot, extract_text) using isolated Playwright contexts
- ✅ web_search via Tavily with normalized title/url/snippet output, safe error paths, and Bearer-token isolation
- ✅ All 9 core tools registered and immediately available to the agent loop
- ✅ Browser auto-closes on shutdown; TMP dir ensured at startup
- ✅ Bot `/start` + `/help` updated to advertise live agent + sessions + tools
- ✅ Dedicated unit tests added: `tests/tools/files.test.ts`, `tests/tools/terminal.test.ts`, `tests/tools/search.test.ts`
- ✅ All 74 tests green, strict TypeScript build clean

### ✅ Milestone 7: Telegram Task Summary

- ✅ task summary structure (title, result, tools used, caveats, next steps) per ui-context.md
- ✅ summary builder `buildTaskSummary` in `src/summary/mod.ts` consuming AgentResult + userRequest
- ✅ credential sanitization via `sanitizeString` inside the builder (and on main content delivery)
- ✅ Telegram chunked delivery using existing `sendSafe` + "Working on it...", "Done.", "Done with issues."
- ✅ 9 dedicated unit tests in `tests/summary/summary.test.ts` (structure, caveats, sanitization, edge cases)
- ✅ Wired into message handler + DI; /help and /status updated; 83 tests green

### ✅ Milestone 8: Railway Deployment

- ✅ Multi-stage `Dockerfile` (Node 20 builder, Playwright Jammy runtime, dev deps pruned, `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` in builder)
- ✅ Playwright/Chromium dependencies bundled via `mcr.microsoft.com/playwright:v1.52.0-jammy`
- ✅ `.dockerignore` excludes secrets, runtime data, tests, docs
- ✅ Railway service deploys from GitHub `main` (auto-deploy on push)
- ✅ Persistent volume attached and mounted at `/data`
- ✅ All required env vars configured in Railway Variables; missing-var crash on first deploy confirmed fail-fast behavior works
- ✅ Custom `SOUL.md` uploaded to `/data/SOUL.md` via `railway ssh "cat > /data/SOUL.md" < ./SOUL.md` after registering the local SSH key with Railway
- ✅ Production startup validated: env loaded, SOUL loaded from volume, browser initialized, bot polling, owner can interact

## Open Questions

- 🔵 Resolved: Confirmed OpenAI model IDs — `OPENAI_PRIMARY_MODEL=gpt-5.4-nano`, `OPENAI_FALLBACK_MODEL=gpt-5-mini`.
- 🔵 Resolved: Use Tavily as the direct web search provider for this stack.
- 🔵 Resolved: v1 will use Telegram-only task summaries. No GitHub repo, no file persistence for reports.
- 🔵 Resolved (M6): Initial terminal allowlist scoped to safe diagnostics — `pwd, ls, echo, date, node, npm, which, cat, head, tail, wc, find`. Will broaden only when a real workload requires it.
- 🔵 Resolved (M3): Missing `/data/SOUL.md` creates a safe default template and logs a warning (does not fail startup).
- 🔵 Resolved (M8): First Railway deployment uses Telegram long polling. Webhook mode can be added later if a public HTTPS endpoint is justified.
- 🔵 Resolved (M8): `MAX_AGENT_ITERATIONS` now drives a true multi-round tool loop. Each iteration is one model call; fallback is first-iteration-only.
- 🔵 Resolved (M8): Task response collapsed to a single merged Telegram message (answer + footer) instead of `Working on it...` / answer / `Done.` + summary. Structured `buildTaskSummary` retained for potential future reuse.

## Architecture Decisions

- Use TypeScript and Node.js for a typed Telegram bot runtime.
- Use GrammY for Telegram because it is lightweight, mature, and TypeScript-friendly.
- Use the OpenAI SDK directly; do not use OpenRouter.
- Keep model IDs in environment variables because public display names may not match API identifiers.
- Use Tavily for direct web search.
- Store persistent agent data under Railway volume path `/data`.
- Limit file tool access to `/tmp/agent-files`.
- v1 task summaries are Telegram-only; durable report archives to GitHub or any external store are explicitly out of scope until context files are updated.
- Enforce Telegram user whitelist before all command and message handling.
- Centralize credential sanitization and run it across logs, tool results, model observations, Telegram responses, and task summaries.

## Session Notes

- The workspace started empty except for `.DS_Store`.
- The six reference files were templates with placeholder sections.
- The created context files are the initial project plan and should guide implementation.
- ✅ 2026-05-30: Completed Milestone 1 (Foundation). Initialized npm + TS, installed grammy/openai/zod/playwright/vitest, full src/ tree, Zod env with safe errors + redaction, Vitest + passing tests, tsc build green, tsx dev, sanitized startup logs, .env.example + strict .gitignore. Ready for Milestone 2.
- ✅ 2026-05-30: Completed Milestone 2 (Telegram Bot Shell). GrammY bot, owner whitelist middleware (first), /start /help /status /reset /model shells, pure chunkMessage + sendChunked, long-polling + graceful shutdown wiring in index.ts. 16 tests passing. Non-owners silently dropped. No agent/tools yet.
- ✅ 2026-05-30: Completed Milestone 3 (SOUL + Sessions). Storage helpers, SOUL loader with safe default creation + warn, full session CRUD under /data/sessions, real /reset command, user message history capture, DI into bot. 33 tests green. SOUL now always present at startup.
- ✅ 2026-05-30: Completed Milestone 4 (OpenAI Agent Loop). OpenAI client wrapper, security-first prompt builder (rules > SOUL), runner with fallback, real agent responses for normal messages, full /model switching (primary/fallback/custom/clear) persisted per chat, history fed to model. 38 tests green. Owner messages now get actual model output.
- ✅ 2026-05-30: Completed Milestone 5 (Tool Registry). Defined Tool interface + ToolResult, full ToolRegistry with Zod validation + OpenAI schema conversion, timeout + truncation wrapper, centralized sanitization (redacts keys/tokens), and agent runner now handles tool_calls + tool results. 48 tests. Registry is live and ready for tools to be registered in M6.
- ✅ 2026-05-30: Completed Milestone 6 (Core Tools). Implemented and registered: file_read/write (path-safe), terminal_exec (narrow allowlist), 5 browser tools (Playwright isolated contexts), web_search (Tavily). Agent can now use real tools. Browser lifecycle managed.
- ✅ 2026-05-30: M6 hardening + closure. Fixed terminal cwd sibling-prefix bypass with explicit `path.sep` check (`resolveSafeCwd`). Wired `MAX_TOOL_OUTPUT_CHARS` into `ToolRegistry.configure()`. Refreshed bot `/start` + `/help` copy to advertise live agent, sessions, and 9 tools. Added dedicated suites: files (9), terminal (12), search (5 with mocked fetch). 74 tests green, strict TS build clean. M6 is now considered fully complete.
- ✅ 2026-05-30: Completed Milestone 7 (Telegram Task Summary). Implemented `src/summary/mod.ts` with `buildTaskSummary` (title derivation, one-paragraph result, tools list, caveats for fallback/failures/limits, conservative next-steps). Full sanitization on summary + main content. Integrated "Working on it..." + "Done."/"Done with issues." delivery in message handler with DI. Updated /help and /status. 9 new tests (structure, sanitization, edges) + full suite at 83 passing. All invariants respected. M7 complete; ready for M8 Railway deploy.
- ✅ 2026-06-01 / 2026-06-02: Completed Milestone 8 (Railway Deployment) and shipped two follow-up improvements driven by real owner usage:
  - **Deploy:** wrote multi-stage Dockerfile (Node 20 builder → Playwright Jammy runtime), `.dockerignore`, pushed to GitHub, connected Railway service, attached `/data` volume, configured env vars, registered local SSH key, and uploaded a custom `SOUL.md` over `railway ssh`.
  - **Single merged Telegram message:** the original "Working on it..." / answer / "Done." + structured summary flow read as a duplicate reply on mobile because the summary repeated the answer paragraph. Replaced with one merged message: `(used fallback model: ...)` (optional) + answer + optional `—` footer (`Tools: ...`, `Failed: ...`, iteration-limit notice). Updated `/help` to describe the new shape. The structured `buildTaskSummary` and its 9 tests are retained but no longer wired into the bot.
  - **Multi-round tool loop:** `runAgent` rewritten as a true bounded `for` loop over `MAX_AGENT_ITERATIONS` so the agent can complete real workflows (navigate → fill email → fill password → click submit → navigate to dashboard → screenshot → extract text). Fallback to `OPENAI_FALLBACK_MODEL` is now first-iteration-only — once any tool result is in the conversation, the active model is locked. `iterationLimitHit` is surfaced in the merged-message footer.
  - **Test credentials feature:** `TEST_ACCOUNT_EMAIL` / `TEST_ACCOUNT_PASSWORD` env pair (already scaffolded in `src/agent/prompt.ts` + `src/index.ts`) confirmed working in production. The agent uses them silently inside `browser_fill` and refuses any flow that would require the owner to send credentials in chat. Both values registered with the central sanitizer.
  - All 94 tests still pass; strict TypeScript and lint clean. Bot is online for the whitelisted owner.
- ✅ 2026-06-01: Pinned Playwright **1.60.0** in `package.json` + `mcr.microsoft.com/playwright:v1.60.0-jammy` runtime image (was 1.52.0 in Docker vs 1.60.0 from npm lock — browser version mismatch in production).
- ✅ 2026-06-??: Completed Phase 1 (Assertion Tools) from `context/improvements/qa-agent-roadmap.md`. Created `src/tools/qa/assertions.ts` (5 tools), exported browser helpers (`getOrCreatePage`, `resolveLocatorAcrossFrames`, new `resolveVisibleLocator` + `isVisibleAcrossFrames`), wired registration in `src/index.ts`, added `tests/tools/qa-assertions.test.ts` (covers pass/fail/missing/cross-frame using setContent + srcdoc iframes for isolation). `npm run build && npm run test:run && npm run typecheck` verified. Updated roadmap + this tracker. 107 tests passing. Foundation for future QA report builder (Phase 2). No invariants violated (sandboxing, sanitization not impacted as assertions produce only DOM-derived pass/fail data).

## Dashboard Progress (from context/dashboard.md)

Private, read-only Next.js dashboard for the Telegram QA agent. Railway stays the source of truth for reports/screenshots; Vercel reads through a small bearer-token-protected Railway API. See `context/dashboard.md` for the full milestone plan, security rules, and planned interfaces.

- ✅ Milestone 0: Planning Branch And Plan File — on branch `feature/dashboard`, `context/dashboard.md` added as docs-only source of truth (current state, V1 architecture, security rules, Milestones 0–12, future demo-chat direction). No runtime code, no scaffold, no API routes, no production changes. Verified `git status --short --branch` on `feature/dashboard` with only docs changed and no secrets/`.env`/`/data`/`/tmp`/screenshots staged.
- ✅ Milestone 1: Context And Scope Update — brought the private dashboard v1 officially into scope across context docs (docs-only; no code/deps/scripts). `project-overview.md`: added "Private Dashboard (v1, in progress)" feature, In-Scope items, and refined Out-of-Scope to separate private read-only dashboard from public SaaS + future demo chat. `architecture.md`: added "Dashboard Architecture (v1)" (Railway API ↔ Vercel relationship, planned endpoints + env), `/data/qa-artifacts/screenshots/` to storage model, and 5 new dashboard invariants (#11–15) that extend (never weaken) existing ones. `ui-context.md`: added "Dashboard UI (v1, in progress)" surface + "Future Demo Chat UI" separation. All existing security invariants kept intact.
- ✅ Milestone 2: Backend Report Contract — added `src/dashboard/report-contract.ts` with Zod schemas (`dashboardReportSchema`, entry/summary/screenshot shapes), lenient `parseStoredQaReportJson` for on-disk JSON, `normalizeStoredQaReport` read model (`id`, `title`, `project`, `startedAt`, `endedAt`, `durationMs`, `summary`, `entries`, `screenshots`), project inference from explicit `project` → `targetUrl` → URLs in title/entries → `unknown`, old-report compatibility (missing summary/screenshots/project), and 11 tests in `tests/dashboard/report-contract.test.ts`. No HTTP routes, screenshot serving, or frontend. `npm run build`, `npm run typecheck`, and dashboard tests pass.
- ✅ Milestone 3: Durable Screenshot Artifacts — added `src/storage/qa-artifacts.ts` with durable storage under `/data/qa-artifacts/screenshots/`, path safety helpers (`resolveSafeArtifactPath`, `resolveDurableScreenshotPath`, filename/report-id sanitization), and `copyTmpScreenshotToDurable`. `saveQaReport` now optionally copies transient TMP screenshots into report-scoped durable folders and persists `{ filename, path, label }` metadata on saved report JSON; `deliverQaArtifacts` wires this in. Transient `TMP_DIR/screenshots` + Telegram delivery unchanged. `ensureDataDirectories` / `getStoragePaths` include artifact dirs. Tests in `tests/storage/qa-artifacts.test.ts` + extended `qa-report.test.ts` / `storage.test.ts`. `npm run build`, `npm run typecheck`, and milestone tests pass.
- ✅ Milestone 4: Railway Dashboard API Foundation — added `src/dashboard/server.ts` (Node built-in `http`) with a pure, testable `routeDashboardRequest`, constant-time bearer auth (`timingSafeEqual`), unauthenticated `GET /health` liveness probe, and bearer-protected `GET /api/dashboard/health`. Responses sanitized before write; `no-store` cache headers. New env vars in `src/config/env.ts`: `DASHBOARD_API_ENABLED` (proper boolean parsing — `"false"` stays false), `DASHBOARD_API_TOKEN` (required, min 16 chars when enabled, enforced via `superRefine`), `DASHBOARD_API_PORT`, and Railway `PORT` fallback (`resolveDashboardPort`). Server starts only when enabled, registers the token as a sanitizer secret, and is wired into graceful shutdown alongside the bot; bot long-polling unchanged. Tests: `tests/dashboard/server.test.ts` (routing + missing/invalid/valid token + live HTTP integration + post-close) and new dashboard cases in `tests/config/env.test.ts`. No report/screenshot routes or frontend yet. `npm run typecheck`, `npm run build`, and dashboard/env tests pass (only pre-existing Playwright browser tests fail without `npx playwright install`).
- ✅ Milestone 5: Railway Reports API — added `src/dashboard/reports-api.ts` to load `/data/qa-reports/`, normalize via `parseStoredQaReportJson` + `normalizeStoredQaReport`, skip corrupt files, and expose project aggregation plus cursor pagination (`base64url` of `startedAt` + `id`, default limit 20, max 100). Extended `src/dashboard/server.ts` with authenticated `GET /api/dashboard/projects`, `GET /api/dashboard/reports?project=&limit=&cursor=`, and `GET /api/dashboard/reports/:id`; async routing with sanitized JSON responses. `startDashboardServer` now requires `dataDir` (wired from `env.DATA_DIR` in `src/index.ts`). Tests: `tests/dashboard/reports-api.test.ts` (sorting, project filter, cursor/limit, corrupt ignored, old-report compatibility, get-by-id) and extended `tests/dashboard/server.test.ts` (auth required, list/filter/detail/404, HTTP integration). No screenshot binary routes or frontend yet. `npm run typecheck`, `npm run build`, and dashboard tests pass.
- ✅ Milestone 6: Railway Screenshot API — added `src/dashboard/screenshots-api.ts` (`listReportScreenshots`, `readDashboardScreenshot`) serving only files under `/data/qa-artifacts/screenshots/` with `resolveSafeArtifactPath` containment, unknown-report rejection, unsupported-extension rejection, and explicit `..`/`/` rejection in filenames. Extended `src/dashboard/server.ts` with `GET /api/dashboard/reports/:id/screenshots` (JSON metadata) and `GET /api/dashboard/screenshots/:reportId/:filename` (binary `image/png` or `image/jpeg`, `Cache-Control: private, max-age=300`); route results now support `json` and `binary` kinds via `writeDashboardRouteResult`. Small `report-contract` fix infers durable screenshot `path` as `<reportId>/<filename>` when only filename metadata is stored. Tests: `tests/dashboard/screenshots-api.test.ts` (8) + extended `server.test.ts` (auth, valid image, missing file, invalid extension, traversal, HTTP integration). No frontend yet. `npm run typecheck`, `npm run build`, and dashboard tests pass.
- ✅ Milestone 7: Next.js Dashboard Scaffold — converted the repo to npm workspaces (`workspaces: ["apps/*"]` in the root `package.json`, plus `dashboard:dev/build/start/lint` convenience scripts; backend scripts unchanged). Added `apps/dashboard` (`@cod3mate/dashboard`): Next.js 16 App Router + React 19 + TypeScript (strict, `@/*` path alias), Tailwind CSS v4 (no config file — `@import "tailwindcss"` + `@theme inline` dark-first tokens in `src/app/globals.css`, `@tailwindcss/postcss`), shadcn/ui base setup (`components.json`, `cn` util in `src/lib/utils.ts`, base `Button`), Clerk dependency (`@clerk/nextjs`, not yet wired — that's M8), and lazy Zod env validation in `src/lib/env.ts` (`getDashboardEnv()` — never imported by placeholder pages so the build needs no secrets). Placeholder pages only: `/`, `/reports`, `/reports/[id]` (all read-only, no Railway API calls). `apps/dashboard/.env.example` documents the dashboard env contract; `.gitignore` excludes `.env*`. Backend `typecheck`/`build`/tests unaffected (195 pass; the 34 failures are the pre-existing Playwright browser tests needing `npx playwright install`). Dashboard `next build` + `tsc --noEmit` pass. Next: Milestone 8 (Clerk-Protected Dashboard Shell).
- ✅ Milestone 8: Clerk-Protected Dashboard Shell — `@clerk/nextjs` v7 (current SDK) wired into `apps/dashboard`. Clerk middleware at `src/proxy.ts` (Next.js 16 uses `proxy.ts`, not `middleware.ts`) protects every route except `/sign-in(.*)` / `/sign-up(.*)` via `auth.protect()`. `ClerkProvider` (with `dynamic` + the `@clerk/ui` `shadcn` theme, CSS imported in `globals.css`) is mounted inside `<body>` in the root layout. Owner allowlist enforced server-side in `src/lib/auth.ts` (`checkOwner`/`isOwner` against `DASHBOARD_ALLOWED_CLERK_USER_ID` or `DASHBOARD_ALLOWED_EMAIL`, fail-closed). Report pages moved into a `(dashboard)` route group whose layout gates on owner status (signed-in non-owner → "Access restricted" state with sign-out) and renders a top bar with `UserButton`. Custom auth pages at `app/sign-in/[[...sign-in]]` and `app/sign-up/[[...sign-up]]`. The private screenshot proxy route re-checks `isOwner()` (404 for non-owners). `.env.example` documents `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`SIGN_UP_URL` + fallback redirect URLs. Dashboard `build` + `lint` + `tsc --noEmit` clean. Next: Milestone 12 (Deployment).
- ✅ Milestone 9: Dashboard API Client — server-only Railway client in `apps/dashboard/src/lib/`: Zod response contracts (`api-contract.ts`), typed errors (`api-errors.ts`, codes `unavailable`/`auth`/`invalid_response`/`not_found`/`unknown`), testable core (`api-client-core.ts`), and `server-only` entry (`api-client.ts`) with `createDashboardApiClient`, `getDashboardApiClient`, and helpers `listProjects` / `listReports` / `getReport` / `getReportScreenshots`. Bearer token sent only in server-side `Authorization` headers; `getRailwayApiEnv()` validates `DASHBOARD_API_BASE_URL` + `DASHBOARD_API_TOKEN` without requiring Clerk (M8 deferred). 9 tests in `tests/dashboard/frontend-api-client.test.ts` (mocked `fetch`: success, auth failure, not found, unavailable, invalid schema). Root script `dashboard:test`. No UI wired yet. Next: Milestone 10 (Reports Overview UI).
- ✅ Milestone 10: Reports Overview UI — `/` is now the reports dashboard (Server Component, `force-dynamic`); `/reports` redirects to `/`; detail stays at `/reports/[id]`. Server-only loader `loadReportsOverview(project)` fetches projects + recent reports (limit 50) via the M9 API client, computes summary (total runs, run-level pass rate, failed checks, latest run), and never throws — connection/auth/schema failures normalize to a typed error result rendered as a calm error state. UI: project filter chips (`?project=`), 4 stat cards, full-row-clickable responsive reports table with pass/fail status badges, empty state, error state, and a `<Suspense>` skeleton fallback. Applied the emil-design-eng principles: custom `--ease-out`/`--ease-in-out` tokens, short staggered fade-up entrances (`prefers-reduced-motion` aware), `active:scale-[0.97]` button press feedback, transform/opacity-only motion, deterministic UTC timestamps to avoid hydration drift. Dashboard `next build` + `lint` + `tsc` clean; backend `typecheck` unaffected. Manual desktop/mobile viewport checks pending (run `npm run dashboard:dev`). Next: Milestone 11 (Report Detail + Screenshot Gallery UI).
- ✅ Milestone 11: Report Detail And Screenshot Gallery UI — `/reports/[id]` now renders a real report (Server Component, `force-dynamic`, wrapped in `<Suspense key={id}>` with a skeleton). Server-only `loadReportDetail(id)` (`src/lib/report-detail.ts`) calls the M9 client's `getReport` once (the report already carries screenshot metadata), computes `now` in the loader for React Compiler purity, and **never throws** — a missing report maps to a typed `not_found` result and connection/auth/schema failures map to a calm error state. Screenshots load through an authenticated server-side proxy route (`/api/screenshots/[reportId]/[filename]`) that fetches the bearer-protected Railway binary endpoint on the server, so `DASHBOARD_API_TOKEN` never reaches the browser; the proxy validates the filename (image extension, rejects `..`/`/`/`\\`) before calling upstream. UI: detail header (title, project, started, duration, checks summary, status badge), a "Failed checks" section shown first when failures exist + full "All checks" list, assertion details rendered safely in `whitespace-pre-wrap break-words` blocks (React-escaped, Railway-sanitized), and a responsive screenshot gallery (proxied `<img>`, click opens full image in a new tab) with an empty state. Old/partial reports degrade gracefully (missing duration/screenshots/skipped). New `NotFoundState`. Dashboard `next build` + `lint` + `tsc --noEmit` clean. Manual viewport QA (no screenshots / multiple screenshots / failed assertions / old JSON) pending via `npm run dashboard:dev`. Next: Milestone 8 (Clerk-Protected Dashboard Shell) — must land before M12 deploy.
- ⬜ Milestone 12: Deployment

## QA Roadmap Progress (from context/improvements/qa-agent-roadmap.md)

- ✅ Phase 1: Assertion Tools
- ✅ Phase 2: QA Report Builder (depends on 1) — collector, auto-wiring from assertions, Telegram emission + /data persistence, /qa-history command, full tests
- ✅ Phase 3: Network & Console Observation — passive Playwright listeners in browser/mod for console (error/warn), network failures (4xx+ + aborted), responses (w/ truncated bodies); 3 qa_ tools; clear on nav/reset; tests for capture + clear + truncation.
- ✅ Phase 4: Wait Tools — browser_wait_for (w/ appear/disappear states + cross-frame), network_idle, wait_for_text; elapsed time reporting; registered + tested.
- ✅ Phase 5: Raise Iteration Limit for QA — `/qa-test`, `QA_MAX_ITERATIONS` env, shared `processAgentTask`.
- ✅ Phase 6: Terminal expansion — grep, curl, npx, git with guards.
- ✅ Phase 7: Reusable scenarios — `/qa-scenarios`, `/qa-run`, `qa_save_scenario`.
- ✅ Phase 8: Multi-app credentials — `TEST_CREDENTIALS_<APP>_*`.
- ✅ Phase 9: Viewport — `browser_set_viewport`.
- ✅ Phase 10: Accessibility — `qa_accessibility_audit` + axe-core.
- ✅ Post-roadmap hardening: `qa_assert_status`, `/qa-report`, extended scenario steps, `QA_MAX_ITERATIONS`, session QA snapshots, docs/SOUL/architecture sync, tighter `npx` + blocked `git config`.

- ✅ 2026-06-??: Completed Phase 2 (QA Report Builder). `src/tools/qa/report.ts` (collector + exact formatQaReport + record from assertions), `src/storage/qa-reports.ts`, wired start/end around every agent run in bot.ts (report only sent/persisted when assertions actually used; sanitized; works on error paths too). Optional `name` on qa_assert tools for nice labels. /qa-history command + help/status updates. 114 tests, clean build/typecheck. All invariants respected (sanitization on output, no new secret paths, /data under ensure). Ready for Phase 3 (console/net) or Phase 5 (tagging + iter bump).
- ✅ 2026-06-??: Completed Phase 3 (Network & Console Observation). Listeners for console (error/warn), requestfailed, response (w/ 4KB truncate + 4xx capture) attached in getOrCreatePage + cleared in navigate/reset. 3 new qa_check_ / qa_intercept_ tools in src/tools/qa/monitoring.ts (query current captured). Registered + help updated. 6 new tests using route/evaluate for hermetic sim (now 120 total). All checks green. Passive, no active intercept needed.
- ✅ 2026-06-??: Completed Phase 4 (Wait Tools). Added waitForAcrossFrames helper + 3 browser_wait_* tools (default 10s, max 30s, return elapsed for QA reports). Reuses cross-frame text resolver. Registered in index, listed in /help. New browser-wait.test.ts (6 tests: appear/timeout/hidden/text/network + elapsed). 126 tests, clean build/typecheck. No flakiness primitives before; now agent can do nav → wait → assert reliably.
- ✅ 2026-06-08: Completed Dashboard Milestone 0 (Planning Branch And Plan File). Working on branch `feature/dashboard`; `context/dashboard.md` is the docs-only source of truth for the private read-only Next.js QA dashboard (current state, V1 Railway+Vercel architecture, security rules, Milestones 0–12, planned Railway API + env contracts, future portfolio demo-chat direction). No runtime code, no Next.js scaffold, no API routes, no production behavior changes. Verified branch + docs-only diff with no secrets/`.env`/`/data`/`/tmp`/screenshots staged. Added Dashboard Progress section above. Next: Milestone 1 (Context And Scope Update) — bring the dashboard into scope across `project-overview.md`, `architecture.md`, `ui-context.md`, and add new dashboard invariants (Clerk-protected, server-to-server Railway token, private screenshot artifacts, no dashboard-triggered agent/tool execution in v1).
- ✅ 2026-06-08: Completed Dashboard Milestone 1 (Context And Scope Update). Docs-only; no code, dependencies, or package scripts changed. Updated three context files: (1) `project-overview.md` — new "Private Dashboard (v1, in progress)" feature section, added In-Scope dashboard items, and rewrote the relevant Out-of-Scope lines so a private read-only owner-only dashboard is clearly in scope while public/SaaS dashboard and the future portfolio demo chat stay out of scope. (2) `architecture.md` — new "Dashboard Architecture (v1)" section (Railway API ↔ Vercel server-side relationship, planned read-only endpoints, planned backend + Vercel env), added `/data/qa-artifacts/screenshots/` to the Storage Model, and added dashboard invariants #11–15 (private Clerk-protected dashboard + server-to-server token, token never in browser, private screenshot artifacts via safe path resolution, read-only/no dashboard-triggered tool execution, sanitized responses that never weaken existing invariants). (3) `ui-context.md` — Interface Surface now notes the second private read-only surface, added "Dashboard UI (v1, in progress)" (screens `/` and `/reports/[id]`, Clerk + owner allowlist, dense/calm/dark-first, private screenshots, old-report compatibility) and a "Future Demo Chat UI" separation. All existing security invariants left intact. Next: Milestone 2 (Backend Report Contract) — first code milestone (Zod schemas + report normalization + tests; no HTTP routes).
- ✅ 2026-06-09: Completed Dashboard Milestone 2 (Backend Report Contract). Added `src/dashboard/report-contract.ts`: Zod schemas for the dashboard read model, lenient `parseStoredQaReportJson`, `normalizeStoredQaReport`, URL helpers, and project inference (`project` → `targetUrl` → hostname from title/entry URLs → `unknown`). Old saved reports without `summary`, `screenshots`, or `project` normalize cleanly; optional screenshot metadata maps to `{ filename, label? }`. Added 11 tests in `tests/dashboard/report-contract.test.ts`. No HTTP routes, screenshot serving, or frontend. `npm run build`, `npm run typecheck`, and dashboard tests pass. Next: Milestone 3 (Durable Screenshot Artifacts).
- ✅ 2026-06-09: Completed Dashboard Milestone 3 (Durable Screenshot Artifacts). Added `src/storage/qa-artifacts.ts` for `/data/qa-artifacts/screenshots/` with containment checks (`resolved root + path.sep`), report-id/filename sanitization, and `copyTmpScreenshotToDurable`. Extended `saveQaReport` + `StoredQaReport.screenshots` metadata; `deliverQaArtifacts` passes `tmpDir` + queued screenshot paths at persist time. Telegram screenshot delivery and transient TMP behavior preserved. Storage paths + `ensureDataDirectories` updated. New/extended tests cover path safety, durable copy, and screenshot metadata roundtrip through `normalizeStoredQaReport`. Next: Milestone 4 (Railway Dashboard API Foundation).
- ✅ 2026-06-09: Completed Dashboard Milestone 6 (Railway Screenshot API). Added `src/dashboard/screenshots-api.ts` for private screenshot serving: `listReportScreenshots` returns normalized metadata for a known report; `readDashboardScreenshot` reads binaries only from `/data/qa-artifacts/screenshots/<reportId>/` using `sanitizeReportId`, `isAllowedScreenshotFilename`, and `resolveSafeArtifactPath`, rejecting unknown reports, missing files, unsupported extensions, and filenames containing `..` or `/`. Extended `src/dashboard/server.ts` with authenticated `GET /api/dashboard/reports/:id/screenshots` and `GET /api/dashboard/screenshots/:reportId/:filename`; binary responses use `Content-Type` + `Cache-Control: private, max-age=300` without JSON sanitization on the body. `DashboardRouteResult` is now a `json` | `binary` union handled by `writeDashboardRouteResult`. Tweaked `parseStoredQaReportJson` to infer screenshot `path` as `<reportId>/<filename>` when only filename metadata exists (old/partial report JSON). Tests: 8 in `screenshots-api.test.ts` + 5 new screenshot cases in `server.test.ts` (47 dashboard tests total). No frontend (Milestone 7). Next: Milestone 7 (Next.js Dashboard Scaffold).
- ✅ 2026-06-09: Dashboard access model changed from owner-only to public portfolio. Removed `DASHBOARD_ALLOWED_CLERK_USER_ID` / `DASHBOARD_ALLOWED_EMAIL` (owner allowlist); any signed-in Clerk user may view. Simplified `auth.ts` to `isSignedIn()` only; removed owner gate from `(dashboard)/layout.tsx` and deleted `auth-states.tsx`. (Briefly added a `DASHBOARD_PUBLIC_PROJECTS` curation layer — see the reversal note below.)
- ✅ 2026-06-09: Removed dashboard curation entirely (owner decision). The short-lived `DASHBOARD_PUBLIC_PROJECTS` / `src/lib/curation.ts` layer was deleted: any signed-in user now sees ALL reports and screenshots (no per-project allowlist). Reverted `reports-overview.ts` (no filtering), `report-detail.ts` (no not_found gate), and `/api/screenshots/...` (signed-in check only, no project gate); removed `DASHBOARD_PUBLIC_PROJECTS` from `env.ts` + `.env.example`; deleted `tests/dashboard/curation.test.ts` and reverted the `dashboard:test` script. Updated `dashboard.md`, `architecture.md` (invariant #11 + env list), `project-overview.md`, `ui-context.md`. **Accepted risk:** screenshots may expose logged-in test-account states; only run QA against projects safe to show publicly while this model holds. Verified dashboard build + lint + typecheck + 9 API-client tests pass.
- ✅ 2026-06-09: Completed Dashboard Milestone 10 (Reports Overview UI). First useful dashboard screen, built with the emil-design-eng craft principles. Routing: `/` became the reports dashboard (Server Component with `export const dynamic = "force-dynamic"`), `/reports` now `redirect("/")` (stable alias), report detail remains `/reports/[id]` (M11). Data: added server-only `src/lib/reports-overview.ts` — `loadReportsOverview(project?)` calls the M9 client (`listProjects` + `listReports({ project, limit: 50 })` in parallel), `computeReportsSummary` (totalRuns, run-level passRate over evaluable runs, failedChecks sum, latestRunIso), and `reportRunStatus` (pass/fail/empty). The loader **never throws**: missing env or `DashboardApiError` map to a typed `{ status: "error", kind, message }` so the UI shows a calm error state instead of a crash; `now` is computed in the loader (not in render) to satisfy the React Compiler purity rule. Added `src/lib/format.ts` (deterministic UTC `formatTimestamp`, `formatRelative`, `formatDuration`, `formatPercent` — server-rendered, no client re-format → no hydration drift). Components under `src/components/dashboard/`: `status-badge` (pass=success / fail=destructive / empty, dot + ring tint), `stat-card` (4 metric cards with lucide icons + tone), `project-filter` (chips linking to `?project=`, `aria-current`), `reports-table` (full-row-clickable Link rows in a shared responsive grid — desktop 6-col, mobile 2-col with stacked meta, truncation everywhere, hover bg + chevron nudge), `states` (empty + error), `reports-skeleton` (Suspense fallback with `animate-pulse`), and `reports-overview` (async composition). Page wraps the overview in `<Suspense key={project}>` so changing the filter re-suspends to the skeleton. Motion per the skill: added `--ease-out`/`--ease-in-out` custom curves + `fade-up`/`.stagger` (50ms steps, ≤280ms) + `prefers-reduced-motion` fallback (opacity-only) to `globals.css`; `Button` got `active:scale-[0.97]` + scoped transitions; only transform/opacity animated. Added `server-only` boundary stays intact (overview + loader are server-only). Verified: `npm run dashboard:build` (routes ƒ `/`, ○ `/reports`, ƒ `/reports/[id]`), `npm run dashboard:lint`, `npm run typecheck --workspace apps/dashboard`, and backend `npm run typecheck` all clean. A live HTTP smoke test wasn't possible in the sandbox (Next's `uv_interface_addresses` host enumeration is blocked), so manual desktop/mobile viewport checks remain for the owner via `npm run dashboard:dev`. No screenshot gallery, public pages, or agent chat (out of scope for M10). Next: Milestone 11 (Report Detail And Screenshot Gallery UI).
- ✅ 2026-06-09: Completed Dashboard Milestone 11 (Report Detail And Screenshot Gallery UI). `/reports/[id]` is now a real Server Component (`export const dynamic = "force-dynamic"`) wrapped in `<Suspense key={id}>` with a dedicated `ReportDetailSkeleton`. Data: server-only `src/lib/report-detail.ts` — `loadReportDetail(id)` makes a single M9 `getReport(id)` call (the report read model already carries `screenshots` metadata), computes `now` in the loader (React Compiler purity), and **never throws**: `DashboardApiError` code `not_found` → typed `{ status: "not_found" }`, all other codes + missing env → calm `{ status: "error", kind, message }`. Screenshots: token must never reach the browser, so they load through a new authenticated server-side proxy route handler `apps/dashboard/src/app/api/screenshots/[reportId]/[filename]/route.ts` that fetches the bearer-protected Railway binary endpoint (`/api/dashboard/screenshots/:reportId/:filename`) server-side and streams it back with `Cache-Control: private, max-age=300`; it validates the filename (image extension only, rejects `..`/`/`/`\\`) before calling upstream (defense in depth on top of Railway's containment checks) and maps upstream 404 → 404, other failures → 502/503. Components under `src/components/dashboard/`: `report-detail` (async composition), `report-detail-header` (title, project, started, duration, checks summary, status badge, started→ended timestamps), `assertion-list` (a "Failed checks" section first when failures exist, then a full "All checks" list; per-entry pass/fail/skip icons; `details` rendered safely in a `whitespace-pre-wrap break-words` mono block — React-escaped + already sanitized by Railway), `screenshot-gallery` (responsive grid of proxied `<img>` thumbnails linking to the full image in a new tab; `ImageOff` empty state for reports without screenshots), `report-detail-skeleton`, plus a new `NotFoundState` in `states.tsx`. Old/partial report JSON degrades gracefully (missing `durationMs`, `screenshots`, `skipped`). Motion follows the M10 emil-design-eng setup (reused `.fade-up`, transform/opacity-only hover scale on thumbnails, existing `--ease-out`). ⚠️ Clerk auth is still deferred (M8); the token stays server-side, which is the critical invariant, and M8's middleware will cover `/api/screenshots` before M12 deploy. Verified: `npm run dashboard:build` (routes ƒ `/`, ƒ `/api/screenshots/[reportId]/[filename]`, ○ `/reports`, ƒ `/reports/[id]`), `npm run dashboard:lint`, and dashboard `tsc --noEmit` all clean; no backend code changed. Manual desktop/mobile viewport QA (no-screenshots, multi-screenshot, failed assertions, old JSON) remains for the owner via `npm run dashboard:dev`. Next: Milestone 8 (Clerk-Protected Dashboard Shell) — hard precondition before Milestone 12 (Vercel deployment).
- ✅ 2026-06-09: Completed Dashboard Milestone 9 (Dashboard API Client). Server-only Railway client for the Next.js dashboard (no UI wired yet). Added `apps/dashboard/src/lib/api-contract.ts` (Zod schemas mirroring the Railway report read model and API envelopes), `api-errors.ts` (`DashboardApiError` with `unavailable`/`auth`/`invalid_response`/`not_found`/`unknown`), `railway-api-env.ts` (`getRailwayApiEnv()` — only `DASHBOARD_API_BASE_URL` + `DASHBOARD_API_TOKEN`, no Clerk keys so M9–M11 can run before M8), `api-client-core.ts` (testable `createDashboardApiClient` with bearer auth, `cache: no-store`, Zod validation), and `api-client.ts` (`import "server-only"` + `getDashboardApiClient` singleton + convenience helpers `listProjects`/`listReports`/`getReport`/`getReportScreenshots`). Token is included only in server-side `Authorization` headers. Added `server-only` dependency and root `dashboard:test` script. Tests: 9 in `tests/dashboard/frontend-api-client.test.ts` (mocked `fetch` — success paths, 401 auth, 404 not found, network/503 unavailable, invalid schema). Verified backend `typecheck`/`build` clean; dashboard `next build` + `dashboard:test` pass. Next: Milestone 10 (Reports Overview UI).
- ✅ 2026-06-09: Completed Dashboard Milestone 8 (Clerk-Protected Dashboard Shell). The dashboard is now private and owner-only. (1) `@clerk/nextjs` is v7 (current SDK), so current Clerk patterns apply (`ClerkProvider` inside `<body>`, async `auth()`/`currentUser()`). Installed `@clerk/ui` and applied the `shadcn` theme to `ClerkProvider` (`appearance={{ theme: shadcn }}`, `dynamic` prop) with `@import "@clerk/ui/themes/shadcn.css"` in `globals.css` so Clerk components match the dark shadcn design system. (2) Clerk middleware lives at `apps/dashboard/src/proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts`): `createRouteMatcher(["/sign-in(.*)","/sign-up(.*)"])` is public, everything else calls `auth.protect()` (signed-out → redirect to `/sign-in`); standard Clerk matcher incl. `/(api|trpc)(.*)` and `/__clerk/(.*)`. (3) Authorization (owner allowlist) is separate from authentication and enforced server-side in `src/lib/auth.ts` — `checkOwner()`/`isOwner()` compare `currentUser()` against `DASHBOARD_ALLOWED_CLERK_USER_ID` and/or `DASHBOARD_ALLOWED_EMAIL` (case-insensitive over the user's verified emails) and fail closed. (4) Moved the three report routes into a `(dashboard)` route group (URLs unchanged: `/`, `/reports`, `/reports/[id]`); the new `(dashboard)/layout.tsx` awaits `checkOwner()` and renders the `Unauthorized` "Access restricted" state (with a Clerk `SignOutButton`) for signed-in non-owners, otherwise a sticky top bar with `UserButton` + children. (5) Custom auth pages: `app/sign-in/[[...sign-in]]/page.tsx` (`<SignIn />`) and `app/sign-up/[[...sign-up]]/page.tsx` (`<SignUp />`), both centered, `force-dynamic`. (6) Defense in depth: the private screenshot proxy route (`/api/screenshots/[reportId]/[filename]`) now re-checks `isOwner()` and returns 404 for non-owners, keeping `DASHBOARD_API_TOKEN` server-side. (7) `apps/dashboard/.env.example` documents `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` + `*_FALLBACK_REDIRECT_URL`. ⚠️ SECURITY: sign-up is publicly reachable by design (custom page) but the owner allowlist blocks any non-owner; restrict sign-ups in the Clerk Dashboard too. Verified: `npm run build`, `npm run lint`, `tsc --noEmit` for the dashboard all clean (routes ƒ `/`, ƒ `/sign-in/[[...sign-in]]`, ƒ `/sign-up/[[...sign-up]]`, ƒ `/api/screenshots/...`, `ƒ Proxy (Middleware)`); no backend code changed. Next: Milestone 12 (Vercel deployment) — now unblocked since M8 is the hard precondition.
- ✅ 2026-06-09: Completed Dashboard Milestone 7 (Next.js Dashboard Scaffold). Frontend app structure only — no dashboard features, no Railway API calls, no backend runtime changes. (1) Converted the repo to npm workspaces: added `"workspaces": ["apps/*"]` to the root `package.json` plus `dashboard:dev` / `dashboard:build` / `dashboard:start` / `dashboard:lint` scripts that proxy to the workspace; existing backend scripts (`build`/`dev`/`start`/`test`/`test:run`/`typecheck`/`clean`) are untouched and the backend `tsconfig` still scopes to `src/**`, so it never picks up dashboard files. (2) Added `apps/dashboard` (`@cod3mate/dashboard`): Next.js 16.2.7 App Router + React 19 + TypeScript strict with `@/*` → `src/*`; Tailwind CSS v4 with **no** `tailwind.config` file (`@import "tailwindcss"` + `@theme inline` dark-first design tokens in `src/app/globals.css`, `@tailwindcss/postcss` via `postcss.config.mjs`); shadcn/ui base setup (`components.json` new-york, `cn` helper in `src/lib/utils.ts`, base `Button` in `src/components/ui/button.tsx`); `@clerk/nextjs` added as a dependency only (provider/middleware deferred to M8); ESLint flat config (`eslint.config.mjs`, `lint` uses `eslint .` since Next 16 removed `next lint`). (3) Lazy Zod env validation in `src/lib/env.ts` (`getDashboardEnv()` with a `superRefine` requiring one of `DASHBOARD_ALLOWED_CLERK_USER_ID` / `DASHBOARD_ALLOWED_EMAIL`) — intentionally **not** imported by any placeholder page so the build needs no real secrets; server-only `DASHBOARD_API_TOKEN` / `CLERK_SECRET_KEY` documented as never client-exposed. (4) Placeholder pages only: `/` (intro + link), `/reports` (empty-state placeholder), `/reports/[id]` (async `params` placeholder). `apps/dashboard/.env.example` documents the dashboard env contract; `apps/dashboard/.gitignore` excludes `.next`, `.env*`, and `next-env.d.ts`. Verified: backend `npm run typecheck` + `npm run build` clean; `npm run test:run` = 195 passing (the 34 failures are pre-existing Playwright browser tests that require `npx playwright install`, unrelated to this milestone); dashboard `next build` succeeds (routes `/`, `/reports`, `/reports/[id]` + `/_not-found`) and `tsc --noEmit` is clean. No `.env`/`/data`/`/tmp`/screenshots staged. Next: Milestone 8 (Clerk-Protected Dashboard Shell) — Clerk provider + middleware + owner allowlist + authenticated/unauthenticated/unauthorized shell states, still read-only and no Railway API calls.
- ✅ 2026-06-09: Completed Dashboard Milestone 5 (Railway Reports API). Added `src/dashboard/reports-api.ts`: reads all JSON under `/data/qa-reports/`, parses with `parseStoredQaReportJson`, normalizes with `normalizeStoredQaReport`, silently skips corrupt files, sorts newest-first by `startedAt` (then `id`), aggregates `listDashboardProjects`, filters by `project`, and paginates with `limit` (default 20, max 100) + opaque `cursor` (`base64url` of `startedAt\x00id`). `sanitizeDashboardResponse` deep-sanitizes payloads. Extended `src/dashboard/server.ts` to async routing for `GET /api/dashboard/projects`, `GET /api/dashboard/reports`, and `GET /api/dashboard/reports/:id` (all bearer-protected; old reports without screenshots/project metadata normalize cleanly). `DashboardServerOptions` now includes `dataDir`; `src/index.ts` passes `env.DATA_DIR` and logs "reports read API". Tests: 9 in `tests/dashboard/reports-api.test.ts` + 14 in `tests/dashboard/server.test.ts` (auth, sorting/filter/cursor, missing report 404, corrupt ignored, HTTP integration). No screenshot binaries or frontend (Milestones 6–7). Next: Milestone 6 (Railway Screenshot API).
- ✅ 2026-06-09: Completed Dashboard Milestone 4 (Railway Dashboard API Foundation). Added the smallest possible HTTP layer (auth + health only) in `src/dashboard/server.ts` using Node built-in `http`: a pure `routeDashboardRequest(req, token)` router (easy to unit test without a socket), `createDashboardRequestListener`, and `startDashboardServer` returning a `{ port, close }` handle for graceful shutdown. `GET /health` is an unauthenticated Railway liveness probe; `GET /api/dashboard/health` requires a server-to-server `Authorization: Bearer <token>` compared with `crypto.timingSafeEqual` (length-guarded, returns 401 on missing/invalid). All response bodies pass through `sanitizeString` before writing, with `Cache-Control: no-store`; non-GET → 405, unknown routes → 404. Env (`src/config/env.ts`): new `DASHBOARD_API_ENABLED` using a custom `envBoolean` preprocessor (fixes the `z.coerce.boolean` footgun so `"false"` is false), optional `DASHBOARD_API_TOKEN`, `DASHBOARD_API_PORT`, and Railway `PORT`; a `superRefine` makes the token mandatory (min 16 chars) whenever the API is enabled, and `resolveDashboardPort` resolves `DASHBOARD_API_PORT → PORT → 8080`. `getEnvSummary` now reports `DASHBOARD_API_ENABLED`, resolved port, and `hasDashboardApiToken` (never the value). `src/index.ts` starts the server only when enabled, registers the token with the sanitizer, logs an enabled/disabled line, and closes it during graceful shutdown next to `stopBot`/`closeBrowser`; bot long-polling untouched. `.env.example` documents the new vars. Tests: `tests/dashboard/server.test.ts` (11 — routing for public/auth routes, missing/invalid/valid/length-mismatch token, 404/405, query-string handling, plus live HTTP integration over `fetch` on an ephemeral port and a post-close rejection) and 4 new dashboard cases in `tests/config/env.test.ts` (default disabled, `"false"` not enabled, enabled-without-token fails fast, enabled+token loads + port resolution + summary). `npm run typecheck` and `npm run build` clean; all dashboard + env tests pass (the only failing suites are the pre-existing Playwright browser tests that require `npx playwright install` and are unrelated to this milestone). No report/project/screenshot routes and no frontend yet (deferred to Milestones 5–7). Next: Milestone 5 (Railway Reports API).
